# When working on this repository

    - Use CodeGraphContext before making architectural assumptions
    - Use find_code for symbol/file discovery
    - Use analyze_code_relationships for callers, callees, inheritance, and dependency relationships
    - Use the graph to identify affected code before refactoring
    - Read the actual source files before modifying them
    - Treat CodeGraphContext as a structural index, not as authoritative source code

---
